package me.boops.mcmods.paths.account;

import spark.Request;

public class AccountCreate {
	
	public String create(Request request) {
		
		// Table layout
		// username -> text
		// email -> text
		// password -> test
		// UUID -> uuid
		
		// To create this table
		// CREATE TABLE users (username text, email text, password text, UUID uuid);
		
		return "";
	}
}
